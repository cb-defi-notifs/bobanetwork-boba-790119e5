import React from 'react'
import { DaoActionContainer } from './style'

interface DaoActionProps {}

export const DaoAction: React.FC<DaoActionProps> = (props: DaoActionProps) => {
  return <DaoActionContainer></DaoActionContainer>
}
